## Summary

<!-- What does this PR change and why? -->

## Spec reference

<!-- SDD: link the spec this implements, e.g. specs/<feature>/spec.md — or write "n/a" for fixes without a spec -->

## Test plan

<!-- Which gate/workflow verifies this? (CI check job, preview E2E, test.yml dispatch, manual) -->

## Checklist

- [ ] `pnpm lint`, `npx tsc --noEmit`, `npx vitest run` pass locally or via `test.yml` on GitHub (local machine is slow — prefer GitHub)
- [ ] Spec updated if behavior changed (`/sdd:amend`)
- [ ] No secrets, tokens, or `.env` content committed
